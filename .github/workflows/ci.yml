on: [push]
jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: GHCVER=8.10 TRAVIS_OS_NAME=linux curl -sSL https://raw.github.com/ndmitchell/neil/master/travis.sh | sh
